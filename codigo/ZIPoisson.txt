##### Zero Inflated Poisson
model {
  for (i in 1:I) {
    
    y[i] ~ dpois(mu2[i])
    
    mu2[i] <- mu1[i]*exposure[i]
    mu1[i] <- eta[i]*I[i] + 0.00001
    
    I[i] ~ dbern(p[i])
    logit(p[i]) <- theta[i]
    
    log(eta[i]) <- beta[1] + beta[2]*x[i]
    theta[i] <- gamma[1]*z[i]
  }
